# public/

Static files served from the site root.

Place the resume here as `Himanshu_Arora_Resume.docx` so the **Download Resume**
buttons (which link to `/Himanshu_Arora_Resume.docx`) resolve. The previous
build hosted this file externally; with the static Next.js export it must be a
real file in this folder.
